server {
  listen 80;

  # Add DNS resolver with short TTL to enable dynamic backend IP updates
  resolver 127.0.0.11 valid=10s ipv6=off;

  location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri $uri/ /index.html;
  }

  location /graphql {
    # Use variable for dynamic backend resolution
    set $backend_url VITE_API_URL_PLACEHOLDER;
    proxy_pass $backend_url;

    # Longer timeouts to support build or larger requests
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}