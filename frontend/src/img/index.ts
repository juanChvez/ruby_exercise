/**
 * Logo image (with brand text).
 * @type {string}
 */
import logo from "./logo.png";

/**
 * Logo image (without brand text).
 * @type {string}
 */
import logoWithoutName from "./logo-without-text.png";

/**
 * Horizontal logo image (wide format).
 * @type {string}
 */
import logoHorizontal from "./logo-horizontal.png";

/**
 * Image assets used throughout the application.
 * 
 * - `logo`: Full logo with brand name text.
 * - `logoWithoutName`: Minimal logo/icon without text.
 * - `logoHorizontal`: Horizontal (wide) logo, suitable for headers.
 * 
 * @module Img
 */
export { logo, logoWithoutName, logoHorizontal };;
